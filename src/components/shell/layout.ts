/**
 * The floating menu card — logo, search and the menu groups — appears on both
 * the company picker and the module home and is meant to read as the same
 * object moving between them. Its width lives here so the two cannot drift
 * apart again.
 */
export const MENU_CARD_WIDTH = 391;
